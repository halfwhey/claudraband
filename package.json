{
  "name": "claudraband-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test packages/claudraband/src packages/claudraband-acp/src packages/claudraband-cli/src",
    "typecheck": "tsc -b"
  }
}
